import React, { useState } from 'react'

import { Body, Container, GlobalStyles, MainWrapper, ResultDiv } from './style'

interface Props {}

const QuizApp: React.FC<Props> = ({}) => {
  return (
    <>
      <GlobalStyles />
      <Body>
        <Container></Container>
      </Body>
    </>
  )
}

export default QuizApp
