import React, { useState } from 'react'

import { Body, Container, GlobalStyles, Genre } from './style'

interface Props {}

const MovieInfo: React.FC<Props> = ({}) => {
  return (
    <>
      <GlobalStyles />
      <Body>
        <Container></Container>
      </Body>
    </>
  )
}

export default MovieInfo
